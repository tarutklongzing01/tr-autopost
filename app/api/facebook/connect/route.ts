import {NextResponse} from 'next/server';import {getFacebookAuthUrl,randomOAuthState} from '@/lib/facebook/auth';
export async function GET(){try{const state=randomOAuthState();const response=NextResponse.redirect(getFacebookAuthUrl(state));response.cookies.set('fb_oauth_state',state,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',path:'/',maxAge:600});return response}catch(error){const message=error instanceof Error?error.message:'Facebook connection is not configured';return NextResponse.redirect(new URL(`/pages?error=${encodeURIComponent(message)}`,process.env.NEXT_PUBLIC_APP_URL||'http://localhost:3000'))}}
