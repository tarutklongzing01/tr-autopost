import {NextRequest,NextResponse} from 'next/server';import {getFacebookAuthUrl,randomOAuthState} from '@/lib/facebook/auth';
export async function GET(req:NextRequest){try{const state=randomOAuthState();const redirectUri=new URL('/api/facebook/callback',req.nextUrl.origin).toString();const response=NextResponse.redirect(getFacebookAuthUrl(state,redirectUri));response.cookies.set('fb_oauth_state',state,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',path:'/',maxAge:600});return response}catch(error){const message=error instanceof Error?error.message:'Facebook connection is not configured';return NextResponse.redirect(new URL(`/pages?error=${encodeURIComponent(message)}`,req.nextUrl.origin))}}
