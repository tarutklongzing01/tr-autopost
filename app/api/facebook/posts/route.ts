import {NextResponse} from 'next/server';import {GRAPH_URL,graphFetch} from '@/lib/facebook/auth';import {getPageAccessToken,listFacebookPages} from '@/lib/facebook/pages';
export const dynamic='force-dynamic';
type MetaPost={id:string;message?:string;created_time:string;permalink_url?:string;full_picture?:string};
export async function GET(){try{const pages=await listFacebookPages();const results=await Promise.allSettled(pages.map(async page=>{const token=await getPageAccessToken(page.pageId);const url=new URL(`${GRAPH_URL}/${page.pageId}/posts`);url.searchParams.set('fields','id,message,created_time,permalink_url,full_picture');url.searchParams.set('limit','10');url.searchParams.set('access_token',token);const data=await graphFetch<{data:MetaPost[]}>(url);return data.data.map(post=>({...post,pageId:page.id,pageName:page.name}))}));const posts=results.flatMap(result=>result.status==='fulfilled'?result.value:[]).sort((a,b)=>new Date(b.created_time).getTime()-new Date(a.created_time).getTime());return NextResponse.json({posts,failedPages:results.filter(x=>x.status==='rejected').length})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Unable to fetch Facebook posts'},{status:500})}}
