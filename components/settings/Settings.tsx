'use client';import {useState} from 'react';import {pages} from '@/lib/mock-data';export function Settings(){const [mock,setMock]=useState(true);return <div className="max-w-3xl"><h1 className="text-2xl font-bold">ตั้งค่า</h1><p className="text-sm text-slate-500 mb-5">ค่าพื้นฐานของพื้นที่ทำงาน</p><div className="card divide-y divide-slate-100"><section className="p-5"><h2 className="font-bold mb-4">ทั่วไป</h2><div className="grid sm:grid-cols-2 gap-4"><label className="text-sm font-semibold">Default Timezone<select className="field mt-1"><option>Asia/Bangkok</option></select></label><label className="text-sm font-semibold">Default Page<select className="field mt-1">{pages.map(p=><option key={p.id}>{p.name}</option>)}</select></label></div></section><section className="p-5 flex justify-between gap-4"><div><h2 className="font-bold">Facebook Mock Mode</h2><p className="text-sm text-slate-500">จำลองการโพสต์โดยไม่เรียก Facebook API จริง</p></div><button aria-label="เปิดปิด Mock Mode" onClick={()=>setMock(!mock)} className={`w-12 h-7 rounded-full p-1 transition ${mock?'bg-blue-600':'bg-slate-300'}`}><span className={`block w-5 h-5 rounded-full bg-white transition ${mock?'translate-x-5':''}`}/></button></section><section className="p-5 flex justify-between"><div><h2 className="font-bold">Scheduler Status</h2><p className="text-sm text-slate-500">ตรวจคิวตาม Vercel Cron</p></div><span className="badge status-published h-fit">● Active</span></section></div></div>}
