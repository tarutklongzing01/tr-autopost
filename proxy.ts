import {NextRequest,NextResponse} from 'next/server';import {verifyAuthMarker} from '@/lib/auth/marker';
export async function proxy(req:NextRequest){try{if(await verifyAuthMarker(req.cookies.get('fbautopost_auth')?.value))return NextResponse.next()}catch{}if(req.nextUrl.pathname.startsWith('/api/'))return NextResponse.json({error:'Unauthorized'},{status:401});const login=new URL('/login',req.url);login.searchParams.set('next',req.nextUrl.pathname);return NextResponse.redirect(login)}
export const config={matcher:['/((?!login|api/auth/session|api/cron/publish|_next/static|_next/image|favicon.ico).*)']};
